Criar um método estático que ira verificar o valor do desconto dado ao apartamento na venda.
O desconto deve estar entre 0% (exclusivo) e 100% (inclusive).

Criar um método que formate os valores do tipo double para uma string com "R$" + valor.
